# Python
__pycache__/
*.pyc
.venv/
venv/

# Credentials — NEVER commit these
service-account*.json
*-key.json
.env

# dbt
dbt/target/
dbt/dbt_packages/
dbt/logs/
dbt/.user.yml          # machine-specific anonymous id
# NOTE: dbt/package-lock.yml IS committed on purpose (pins package versions)

# Terraform
terraform/.terraform/
*.tfstate
*.tfstate.*
terraform.tfvars
*-key.json
sa-key.json
